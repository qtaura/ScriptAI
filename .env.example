# ScriptAI environment configuration (example)
# Copy to `.env` and set your credentials. Do NOT commit real secrets.

# Optional — enables OpenAI backend if provided
OPENAI_API_KEY=

# Optional — enables HuggingFace backend if provided
HUGGINGFACE_API_KEY=